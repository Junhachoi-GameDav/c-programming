/*
#include <iostream>
using namespace std;

class _math {
public:
	static int abs(int a) { return a > 0 ? a : -a; } //a가 0보다 크면 t =a, f =-a
	static int max(int a, int b) { return (a > b) ? a : b; }
	static int min(int a, int b) { return (a > b) ? b : a; }
};

int main() {
	cout << _math::abs(-5) << endl;
	cout << _math::max(10, 8) << endl;
	cout << _math::min(-3, -8) << endl;
}
*/